spring:
  datasource:
    url: jdbc:postgresql://localhost:5400/fixdev_blog
    username: fixdev
    password: fixdev_dev
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: blog
  session:
    store-type: none
